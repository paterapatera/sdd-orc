# Phase Gate

`.agents/skills/sdd-spec/scripts/sdd.py next` decides requirements and design from the review `VERDICT`, the `## Phase Gate` `STATUS`, and the SHA256 fields. The same command's `gate` decides tasks.

Write `VERIFIED` only when the review is `GO` and the recorded hashes match the current files.
