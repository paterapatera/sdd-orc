# Boundaries

全体の境界と依存方向のみ。詳細契約は `docs/contracts/` を参照する。

## 依存方向

<!-- モジュール / レイヤ間の許可された依存だけを短く書く。巨大化したらドメイン別ファイルへ分割する -->

| From | To | Rule |
|------|-----|------|
| _(例: api)_ | _(例: domain)_ | _(例: 一方向のみ)_ |

## 境界メモ

- 契約面の正本は `docs/contracts/` の各ファイル
- 重要判断は `docs/architecture/adr/`
